<h2 class="title">Liste des pages</h2>
  <% @users.each do |user| %>
    <%= link_to "#{user.firstname} #{user.lastname}", public_page_path(user.username) %>
<% if current_user %>
| <%= link_to 'Editer', edit_page_path(user.username) %>
<% end %>
<% end %>