<h1>Liste des profils</h1>
<% @users.each do |user| %>
  <h2>
    <%= link_to "#{user.firstname} #{user.lastname}", public_page_path(user.username) %>
    <% if current_user %> | <%= link_to 'Editer', edit_page_path(user.username) %><% end %>
  </h2>
<% end %>